apiVersion: v1
kind: Service
metadata:
  name: notification-service-service
  namespace: shopping-app
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
  - port: 3002
    targetPort: 3002
  type: ClusterIP


