apiVersion: v1
kind: Service
metadata:
  name: user-service-service
  namespace: shopping-app
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
  - port: 3004
    targetPort: 3004
  type: ClusterIP
